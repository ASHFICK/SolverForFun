#include<bits/stdtr1c++.h>
using namespace std;
int main(){
long long a;
int i,n;
cin>>n;
for(i=1;i<=n;i++){
        cin>> a;
    int b,c;
    b=log(a)/log(2);
    c= a-pow(2,b);
   if (c==0){
    cout<< "no \n";
   }
   else{
    cout<< "yes \n";
   }
        }
}
