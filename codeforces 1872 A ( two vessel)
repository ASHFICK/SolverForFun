//AUTHOR:: ASHFICK
// 17 SEP 2023
#include <bits/stdc++.h>
using namespace std;
void round() {
    int t;
    cin >> t;
    while (t--) {
        int a, b, c;
        cin >> a >> b >> c;
        cout << (abs(a-b) + 2*c - 1) / (2*c) << '\n';
    }
}
int main(){
round();
}
